<!-- TODO: What is that ? == les notices, genre "signed successfull, etc..."
  <p id="notice"><%= notice %></p>
-->
<header class="classic-header">
  <h1><a href="/">Epya</a></h1>
  <nav>
  	<ul>
  	<li>
    <%= form_tag({:controller => 'tags',  :action => 'search'})  do %>
          <%= text_field_tag(:searchTag, nil) %>
          <%= submit_tag("Search", :class => 'btn btn-default') %>
    <% end %>
    </li>
      <li><a href="/projects">Projects</a></li>
      <li><a href="/idea">About</a></li>
      <% if user_signed_in? %>
        <li>
          <%= link_to(current_user.name, user_path(:id => current_user.id)) %>
        </li>
        <li><%= link_to 'New Project', new_project_path %></li>
        <li><%= link_to('Logout', destroy_user_session_path, :method => :delete) %></li>
      <% else %>
        <li><%= link_to('Login', user_session_path)  %></li>
      <% end %>
    </ul>
  </nav>
</header> 
