<%= form_for(@project) do |f| %>
  <% if @project.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@project.errors.count, "error") %> prohibited this project from being saved:</h2>

      <ul>
      <% @project.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="form-group">
    <%= f.label :name, 'Nom' %>
    <%= f.text_field :name, :class => 'form-control' %>
  </div>
  <div class="form-group">
    <%= f.label :description, 'Description' %>
    <%= f.text_area :description, :rows => '25', :class => 'form-control tinymce' %>
  </div>
  <div class="form-group">
    <%= f.label :photo, 'Illustration' %>
    <%= f.text_field :photo, :class => 'form-control' %>
  </div>
  <div class="field">
    <%= label_tag :tag %><br>
    <%= text_field_tag 'tag[]', nil %>
  </div>
    <div class="field">
    <%= label_tag :tag %><br>
    <%= text_field_tag 'tag[]', nil %>
  </div>
    <div class="field">
    <%= label_tag :tag %><br>
    <%= text_field_tag 'tag[]', nil %>
  <div class="form-group">
    <%= f.label :collaborators, 'Les collaborateurs au projet' %>
    <%= f.select(:collaborators, @users.map { |s| [ s.email, s.id ] }, :prompt => "", :class => 'form-control') %>
  </div>
  <div class="actions">
    <%= f.submit 'Valider', :class => 'btn btn-primary' %>
  </div>
<% end %>
