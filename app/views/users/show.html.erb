<h1 class="page-header"> Profil </h1>
		<div class="row">
			<div class="col-md-6">
				<% if user_signed_in? && current_user.id == @user.id %>
					<%= render 'form' %>
				<% else %>
					<p>
						<%= @user.name %>
					</p>
					<p>
						<%= @user.email %>
					</p>
				<% end %>
			</div>
	</div>
